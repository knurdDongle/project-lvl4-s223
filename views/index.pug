doctype html
html(lang="en")
  head
    title= Title
    script.
      var gon = !{JSON.stringify(gon)};
  body
    .container-fluid
      #chat
      // block content
    script(src='/assets/main.js')
